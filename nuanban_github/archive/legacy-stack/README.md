# 历史栈（V1 不再部署）

本目录为 **NestJS + PostgreSQL + Vue Admin** 早期方案，仅供对照参考。

| 目录 | 说明 |
|------|------|
| `api/` | 原 `packages/api`（NestJS） |
| `admin-web/` | 原 `packages/admin-web` |
| `database/` | PostgreSQL schema / seed |

**当前 V1 栈**：`packages/miniapp` + `packages/pocketbase` + Docker Compose。  
新功能与部署请勿依赖本目录。
