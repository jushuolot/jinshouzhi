# 本地测试机（复制为 config/local.env，勿提交）
# cp config/local.env.example config/local.env

# 本地 parity 联调（与阿里云同逻辑）
NUANBAN_API=http://localhost:8090

# 前端: packages/miniapp/.env → VITE_DEMO_MOCK=false（dev-test.sh / start-h5.sh 强制）
# Mock 仅 GitHub Pages；本地用 PocketBase 测试数据
