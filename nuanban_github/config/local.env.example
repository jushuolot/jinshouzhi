# 本地测试机（复制为 config/local.env，勿提交）
# cp config/local.env.example config/local.env

# 本地 PocketBase
NUANBAN_API=http://localhost:8090

# 前端 dev 默认即 development + VITE_DEMO_MOCK=true（见 packages/miniapp/.env.example）
