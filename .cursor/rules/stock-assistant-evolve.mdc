---
description: Stock Assistant 自主进化约定（用户忙时持续迭代）
globs: stock-assistant/**
alwaysApply: false
---

# 暖伴选股 · 自主进化

用户说「自己进化」「按计划进化」「记住进化」时：

## 唯一主线（P125 起）

**预测 → K 线真实验证 → 校准模型 → 再预测**

1. 每次推荐必须写入 log，到期用 **推荐日收盘价 vs D+1~D+3 K 线** 验证（禁止用榜单涨跌幅糊弄）
2. `prediction_calibration.py` 产出命中率、模式权重、门槛调整，反哺 `tomorrow_picks`
3. 展示一只股票 = **全维档案 + 可读结论**（`stock_dossier.py`），不散落功能

## 花园页结构（保持聚焦）

1. 搜一只 → 全维结论
2. 预测 vs 真实（校准面板，常显）
3. 可选：明日预测明细

不要加：cohort 条、多余复盘区、嵌套 expander。

## 交付

- 读 `app_meta.py` → bump 版本 → 测试 + `evolve_verify.sh` → push `main`
- 最小 diff；不提交 secrets

当前基线：**P125 · v6.0.0 · step 1167**
