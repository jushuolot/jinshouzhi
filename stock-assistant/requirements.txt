# UI
streamlit>=1.28.0,<1.40
plotly>=5.18.0,<6

# Data（pandas 2.0 需 numpy 1.x，勿升到 numpy 2 否则会崩溃）
numpy>=1.24.0,<2
pandas>=2.0.0,<2.1
requests>=2.28.0,<3

# Overseas (US/HK/ETF etc.)
# Python 3.8 兼容：固定 yfinance 与 multitasking，避免类型注解兼容问题
multitasking==0.0.11
# yfinance 1.2.2+ 需要 curl_cffi>=0.15（Py3.8 环境常装不到该版本）；固定 1.2.0
yfinance==1.2.0
curl_cffi>=0.7,<0.14

